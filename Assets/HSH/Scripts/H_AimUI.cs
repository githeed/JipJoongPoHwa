using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class H_AimUI : MonoBehaviour
{

    //H_PlayerAttack ha;

    // Start is called before the first frame update
    void Start()
    {
        //ha = GetComponentInParent<H_PlayerAttack>();
    }

    // Update is called once per frame
    void Update()
    {
        //if (ha.dirToTarget != null) // && !pa.isBAttack && !pa.isESkill && !pa.isRSkill
        //{
        //    // dirToTarget = nearestTarget.transform.position - transform.position;
        //    //dirToTarget = pa.dirB;
        //    transform.rotation = Quaternion.LookRotation(-transform.forward, ha.dirToTarget);
        //}
    }
}
